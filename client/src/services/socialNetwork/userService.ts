import { User } from '../../models/socialNetwork/User';

// Fonction de service pour récupérer les informations de l'utilisateur
export const fetchUserProfile = async (userId: string): Promise<User> => {
  // Code pour récupérer les informations de l'utilisateur depuis l'API
};

// Autres fonctions de service pour gérer les utilisateurs si nécessaire
