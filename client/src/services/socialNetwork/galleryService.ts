import { Photo } from '../../models/socialNetwork/Photo';

// Fonction de service pour récupérer les photos de la galerie
export const fetchGalleryPhotos = async (): Promise<Photo[]> => {
  // Code pour récupérer les photos depuis l'API de la galerie
};

// Autres fonctions de service pour gérer la galerie si nécessaire
