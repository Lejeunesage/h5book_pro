import { Game } from '../../models/socialNetwork/Game';

// Fonction de service pour récupérer les jeux disponibles
export const fetchGames = async (): Promise<Game[]> => {
  // Code pour récupérer les jeux depuis l'API
};

// Autres fonctions de service pour gérer les jeux si nécessaire
