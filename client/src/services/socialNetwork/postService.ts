import { Post } from '../../models/socialNetwork/Post';

// Fonction de service pour récupérer les posts
export const fetchPosts = async (): Promise<Post[]> => {
  // Code pour récupérer les posts depuis l'API
};

// Autres fonctions de service pour créer, mettre à jour et supprimer des posts si nécessaire
